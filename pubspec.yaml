name: stream_event_guard
description: Keyed duplicate suppression and cooldown control for synchronous and asynchronous Dart events.
version: 0.1.0-dev.1
repository: https://github.com/rodrigorafaeldamaceno/stream_event_guard
issue_tracker: https://github.com/rodrigorafaeldamaceno/stream_event_guard/issues

topics:
  - async
  - concurrency
  - deduplication
  - events

environment:
  sdk: ^3.12.2

dependencies: {}

dev_dependencies:
  fake_async: ^1.3.3
  lints: ^6.1.0
  test: ^1.31.0
